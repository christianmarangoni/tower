all: # keys must be unique, i.e. only one 'hosts' per gro
  hosts:
    children:
      jboss1
        test1:
        test2:
          vars:
            instancehost: 3a
            instancehost: 2a
      vars:
        instancesgroup: 1a 
